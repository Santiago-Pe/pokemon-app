// src/sections/battleLog/battleLogSection.tsx
import React from "react";
import { DataTbale } from "../../components";

const BattleLogSection = () => {
  // To Do: api call to get list of battles
  // To Do: view if is necesary use table here and intereate data heare.
  return <DataTbale />;
};

export default BattleLogSection;
